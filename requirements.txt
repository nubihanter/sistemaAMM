# Framework base
Django

# Gerenciamento de variáveis de ambiente (.env)
python-decouple

# Banco de dados (PostgreSQL)
psycopg[binary]

# Consumo de APIs REST externas
httpx

# Arquivos estáticos e servidor de produção (para paridade dev/prod)
whitenoise
gunicorn